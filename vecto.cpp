#include<iostream>
#include<vector>
#include<algorithm> 
using namespace std;
int main(){
	/*vector<int> a;
	a.push_back(100); // mo rong ra dang sau
	a.pop_back() : xoa phan tu o cuoi
	
	cout<<a[0]<<endl; 
	cout<<a.size();
	
	for(vector<int>:: iterator it = a.begin(); it != a.end(); ++it)
	cout<<*it<<" ";
	a.begin() + x--> a[x]
	
	cin>>n;
	vector<int> a(n); vetor a co n phan tu
	for(int i=0;i<n;i++) cin>>v[i]
	vector<int> a(n,100) tat cac vector co gia tri bang 100
	
	for (int x : a) cout<< x <<' ';  in ra 
	memset(a, 0 ,sizeof(a)); gi tri cua mang, kich co cua mang,gan dk moi 0 va -1 thoi
	
	a.erase(a.begin()+ x) :xoa phan tu chi so x, can co IT
	a.erase(a.begin()+ x, a.begin()+ y+1): xoa tu vi tri x den y
	a.insert(a.begin()+3, 1000) : chen 1000 vao chi so 3
	
	
	for(int i=0;i<v.size;i++) cout<<v[i]<<" ";
	
	vecto<pair<int,int>> v(n);
	*/
}
